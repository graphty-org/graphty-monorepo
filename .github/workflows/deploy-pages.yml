name: Deploy to GitHub Pages

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    name: Build Documentation
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        env:
          HUSKY: '0'

      - name: Cache Nx
        uses: actions/cache@v4
        with:
          path: .nx/cache
          key: nx-${{ runner.os }}-pages-${{ hashFiles('pnpm-lock.yaml') }}-${{ github.sha }}
          restore-keys: |
            nx-${{ runner.os }}-pages-${{ hashFiles('pnpm-lock.yaml') }}-
            nx-${{ runner.os }}-pages-

      # Build all packages (graphty needs Sentry env vars)
      - name: Build packages
        run: pnpm exec nx run-many -t build --projects=algorithms,layout,graphty-element,graphty
        env:
          VITE_BASE_PATH: /
          VITE_SENTRY_DSN: ${{ secrets.VITE_SENTRY_DSN }}
          VITE_SENTRY_ORG: ${{ secrets.VITE_SENTRY_ORG }}
          VITE_SENTRY_PROJECT: ${{ secrets.VITE_SENTRY_PROJECT }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}

      # Build graphty-element docs (VitePress)
      - name: Build graphty-element docs
        run: pnpm exec nx run graphty-element:docs:build

      # Build Storybooks
      - name: Build graphty-element Storybook
        run: pnpm exec nx run graphty-element:build-storybook

      - name: Build graphty Storybook
        run: pnpm exec nx run graphty:build-storybook

      # Build algorithms gh-pages
      - name: Build algorithms gh-pages
        run: pnpm exec nx run algorithms:build:gh-pages

      # Build layout gh-pages
      - name: Build layout gh-pages
        run: pnpm exec nx run layout:build:gh-pages

      # Assemble the unified GitHub Pages site
      # URL Structure:
      #   / = graphty app (React app - graphty.app)
      #   /docs/ = graphty-element docs (VitePress)
      #   /storybook/element/ = graphty-element Storybook
      #   /storybook/app/ = graphty Storybook
      #   /algorithms/ = algorithms gh-pages
      #   /layout/ = layout gh-pages
      - name: Assemble GitHub Pages site
        run: |
          mkdir -p ./public

          # graphty app as root (/)
          if [ -d "./graphty/dist" ]; then
            cp -r ./graphty/dist/* ./public/
            echo "✅ Copied graphty app to /"
          else
            echo "⚠️ graphty dist not found"
          fi

          # graphty-element docs (/docs/)
          if [ -d "./graphty-element/docs/.vitepress/dist" ]; then
            mkdir -p ./public/docs
            cp -r ./graphty-element/docs/.vitepress/dist/* ./public/docs/
            echo "✅ Copied graphty-element docs to /docs/"
          else
            echo "⚠️ graphty-element docs not found"
          fi

          # graphty-element Storybook (/storybook/element/)
          if [ -d "./graphty-element/storybook-static" ]; then
            mkdir -p ./public/storybook/element
            cp -r ./graphty-element/storybook-static/* ./public/storybook/element/
            echo "✅ Copied graphty-element Storybook to /storybook/element/"
          else
            echo "⚠️ graphty-element Storybook not found"
          fi

          # graphty Storybook (/storybook/app/)
          if [ -d "./graphty/storybook-static" ]; then
            mkdir -p ./public/storybook/app
            cp -r ./graphty/storybook-static/* ./public/storybook/app/
            echo "✅ Copied graphty Storybook to /storybook/app/"
          else
            echo "⚠️ graphty Storybook not found"
          fi

          # Create storybook index page
          cat > ./public/storybook/index.html << 'SBINDEXEOF'
          <!DOCTYPE html>
          <html>
          <head>
            <title>Graphty Storybooks</title>
            <style>
              body { font-family: system-ui, sans-serif; max-width: 800px; margin: 2rem auto; padding: 1rem; }
              h1 { color: #333; }
              ul { list-style: none; padding: 0; }
              li { margin: 1rem 0; }
              a { color: #0066cc; text-decoration: none; font-size: 1.2rem; }
              a:hover { text-decoration: underline; }
              .desc { color: #666; font-size: 0.9rem; margin-top: 0.25rem; }
            </style>
          </head>
          <body>
            <h1>Graphty Storybooks</h1>
            <ul>
              <li>
                <a href="/storybook/element/">graphty-element</a>
                <div class="desc">Web Component library for graph visualization</div>
              </li>
              <li>
                <a href="/storybook/app/">graphty app</a>
                <div class="desc">React application components</div>
              </li>
            </ul>
            <p><a href="/">← Back to graphty.app</a></p>
          </body>
          </html>
          SBINDEXEOF
          echo "✅ Created storybook index at /storybook/"

          # algorithms gh-pages (/algorithms/)
          if [ -d "./algorithms/gh-pages" ]; then
            cp -r ./algorithms/gh-pages ./public/algorithms
            echo "✅ Copied algorithms to /algorithms/"
          else
            echo "⚠️ algorithms gh-pages not found"
          fi

          # layout gh-pages (/layout/)
          if [ -d "./layout/gh-pages" ]; then
            cp -r ./layout/gh-pages ./public/layout
            echo "✅ Copied layout to /layout/"
          else
            echo "⚠️ layout gh-pages not found"
          fi

          echo ""
          echo "📁 Site structure:"
          find ./public -type f -name "*.html" | head -30

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
