// Vite's client types: `import.meta.glob` (the theme's CSS and extension aggregators) and asset
// imports such as `*.woff2`, which resolve to a URL (inlined as a data URL in the library build).
/// <reference types="vite/client" />
