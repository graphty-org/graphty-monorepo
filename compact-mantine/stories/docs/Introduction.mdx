import { Meta } from "@storybook/blocks";

<Meta title="Getting Started/Introduction" />

# compact-mantine

Compact components and a compact theme for [Mantine 8](https://mantine.dev), for
interfaces where screen space is the scarce resource: property panels,
inspectors, sidebars, editors and dashboards. The look is measured from the
Figma editor's own interface, in its light and its dark theme.

The package gives you two things, and you can take either one on its own.

**A theme.** Put `compactTheme` into your `MantineProvider` and the Mantine
components render dense and Figma-accurate -- 24px controls, 11px Inter, Figma's
colours, radii, shadows and timing -- with no `size` prop anywhere in your code.
The theme ships its own typeface and stylesheet. `createCompactTheme({ highContrast: true })`
is the same look with the few colours Figma leaves under WCAG AA contrast raised.

**A component library.** Components built for a narrow column and the editor
around it: rows that fit a value and its label on one 32px line, charts the
height of a line of text, a virtualized data table, a floating-panel system, and
the editor shell -- a floating toolbar with tool flyouts, a navigation rail, a
quick actions palette and a keyboard shortcuts sheet.

## Install

```bash
npm install @graphty/compact-mantine @mantine/core @mantine/hooks
```

Mantine, React and React DOM are peer dependencies, so you choose their
versions. React 18 or later, Mantine 8 or later.

## Five-minute start

Import Mantine's stylesheet, hand `MantineProvider` the compact theme, wrap your
app once in `Tooltip.Group` (for Figma's tooltip timing), and you are done.
Everything else is optional.

```tsx
import "@mantine/core/styles.css";

import { Button, MantineProvider, TextInput, Tooltip } from "@mantine/core";
import { compactTheme, ControlSection, FieldRow, PanelField, ToggleRow } from "@graphty/compact-mantine";

export function App() {
    return (
        <MantineProvider theme={compactTheme}>
            <Tooltip.Group>
                <TextInput label="Project name" />
                <Button>Save</Button>

                <div style={{width: 240}}>
                    <ControlSection label="Node size">
                        <FieldRow>
                            <PanelField label="Smallest" glyph="sizeSmallest" defaultValue="1.0" />
                            <PanelField label="Largest" glyph="sizeLargest" defaultValue="4.0" />
                        </FieldRow>
                        <ToggleRow label="Scale with zoom" defaultChecked />
                    </ControlSection>
                </div>
            </Tooltip.Group>
        </MantineProvider>
    );
}
```

## How this Storybook is organised

Each section below is a job rather than a category, and each one opens with an
overview page explaining when to reach for what is inside it.

- **Building a Panel** -- The containers and the row layout: sections, groups, the field row, the trailing slot, the advanced settings button.
- **Editing a Value** -- Everything the reader can change: fields, toggles, segmented pictures, colours, gradients, ranges.
- **Showing Data** -- Everything the reader can only read: list rows, charts, readings, a virtualized table.
- **Floating Panels** -- Pop-outs: panels that open beside a control, can be dragged, and can nest.
- **Figma** -- Every component's states side by side, as Figma draws them, including the editor shell: toolbar, tool flyouts, secondary bar, navigation rail, help button, shortcuts sheet, quick actions.
- **Glyphs** -- Every drawing the library uses, with its name and its meaning.
- **Compact Theme** -- What the theme does to each Mantine component it restyles, the tokens, and the WCAG AA option.

Use the toolbar above any story to switch between the light and dark schemes,
and between exact Figma and the WCAG AA contrast option. Every component is
built for all of them.

## The ideas worth knowing before you start

**A caption can be a drawing.** Stacking a label above an input costs a line.
Putting a small picture *inside* the input costs nothing, and halves the height
of a panel. That is what `PanelField` does, and the fixed set of pictures it may
use is in the **Glyphs** section.

**Rows agree on a grid.** Every row measures itself from one exported object,
`PANEL_GRID`, which spends Figma's 240px panel column as
`16 + 88 + 8 + 88 + 8 + 24 + 8`: padding, two fields with a gutter between
them, a gap, a 24px trailing slot, padding. That is why a column of unrelated
rows still lines up, and why the trailing slot is drawn even when it is empty.

**Colour comes from a map, never a hex.** `PANEL_INK` names one colour per role
-- primary text, secondary text, surface, accent, border, selected, disabled --
and every entry is one of the theme's `--cm-*` tokens. Paint your own rows from
it and they follow the light scheme, the dark scheme and the AA option without
further work.

**Nothing is required beyond the theme.** Three wrappers exist and all are
opt-in: Mantine's `Tooltip.Group`, once around the app for Figma's warm tooltip
hand-off; `PopoutManager`, needed once near the root if you use pop-outs; and
`LabelsProvider`, if you want to translate the library or format numbers for a
locale.

## Also worth reading

The README in the package covers the same ground in one page and adds the
TypeScript and accessibility notes, the WCAG AA option, server rendering, and the
list of changes a consumer notices in the Figma release.
