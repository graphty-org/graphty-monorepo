# WebGPU graph algorithms and layouts: the design corpus

The package is `webgpu-graph-algorithms/` (`@graphty/webgpu-graph-algorithms`). It was designed and built
(phases P0-P3) in the staging repository graphty-org/webgpu-graph-algorithms and moved here with its git
history on 2026-09-17 (phase M1 of the integration plan below).

| Document                                                                        | What it is                                                                                                                                                                                                                                                                                                        | Status                       |
| ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| `webgpu-acceleration-plan.md` (449 KB)                                          | The accepted design (owner-approved 2026-09-14, plan of record; later changes are appended to its Review log): runtime model, package architecture, memory, kernels, primitives, force-directed layouts, algorithms, integration with the CPU packages, targets, testing, CI, phases and gates, risks, references | live spec                    |
| `superseded-parts-of-the-design.md`                                             | Every passage of the design that a decision record overrules -- where it is, what it says, what is true instead, which record decided it -- and the two questions the decisions left open                                                                                                                         | live index                   |
| `plans/2026-09-14-webgpu-p0-p3-interfaces.md` (325 KB)                          | The interface contract of phases P0-P3: every file, signature, WGSL body and test; the package CLAUDE.md ranks it first                                                                                                                                                                                           | live contract                |
| `plans/2026-09-15-webgpu-p0.md`, `-p1.md` (1.3 MB), `-p2.md`, `-p3.md` (1.2 MB) | The executed phase plans, which embed the files, scripts and gate templates of their time; records, not specs (GitHub does not render the two largest)                                                                                                                                                            | records                      |
| `plans/2026-09-16-graphty-monorepo-integration.md`                              | This move, the build / CI / release wiring, the GPU runner, and the layout integration (design section 9.3). Two cells superseded -- the app's detection work and the M8a entry criterion; its banner names both                                                                                                  | live plan, partly superseded |
| `plans/2026-09-19-webgpu-m8a-algorithms-seam.md`                                | Phase M8a: the graph-format bridge (A1), the first six `indexed.*` ports, `indexed/accelerator.ts` with `accelerated()`, and the GPU package's W1b algorithms half                                                                                                                                                | live plan                    |
| `plans/2026-09-19-webgpu-m8b-gpu-spmv.md`                                       | Phase M8b: the GPU SpMV family, PageRank and its relatives, and Afforest WCC (design P7), gate G7                                                                                                                                                                                                                 | live plan                    |
| `plans/2026-09-20-webgpu-p5-fruchterman-reingold.md`                            | Phase P5: Fruchterman-Reingold and the spring-electrical preset on the exact tier (design P5), gate G5                                                                                                                                                                                                            | live plan                    |
| `plans/2026-09-20-webgpu-p4-grid-pyramid-and-tiers.md`                          | Phase P4: the grid primitives, the degree tiers, windowed execution and the grid pyramid under all three layout models, after P5 on the same branch (design P4), gate G4                                                                                                                                          | live plan                    |
| `plans/2026-09-23-webgpu-p8-frontier.md`                                        | Phase P8: the frontier queue, `advance`, breadth-first search in its two-phase, fused and bottom-up forms, weighted shortest paths, Bellman-Ford and closeness (design P8), gate G8                                                                                                                               | live plan                    |
| `plans/2026-09-23-webgpu-p9-betweenness-and-all-pairs.md`                       | Phase P9: betweenness and edge betweenness by the McLaughlin-Bader passes, and all-pairs shortest paths as a blocked Floyd-Warshall (design P9), gate G9                                                                                                                                                          | live plan                    |
| `plans/2026-09-23-webgpu-p11-structure-and-community.md`                        | Phase P11: building a graph on the device, k-core, triangles and k-truss, label propagation, Boruvka's minimum spanning tree and Louvain (design P11), gate G11                                                                                                                                                   | live plan                    |
| `plans/2026-09-19-webgpu-m6-graphty-element.md`                                 | Phase M6 as first planned against the version 1 element API: the graph-format data model (E0) and the accelerator seam (E1) in graphty-element. Superseded on 2026-09-21 by the re-planned M6 below; kept as history                                                                                              | superseded                   |
| `plans/2026-09-19-webgpu-m7-graphty-app.md`                                     | Phase M7 as first planned against the version 1 element API and the old M6: the app's `attachAccelerator`, the GPU indicator and the Settings control. Superseded on 2026-09-21 by the re-planned M7 below; kept as history                                                                                       | superseded                   |
| `plans/2026-09-21-webgpu-m6-graphty-element-v2.md`                              | Phase M6 (re-planned) on the version 2 element API: master merged into the version 2 branch first, the force layouts driven through the layout simulation seam on the CPU or the GPU, five algorithm adapters through `accelerated()`, one controller shared by the element and its session, gate G6              | live plan                    |
| `plans/2026-09-21-webgpu-m7-graphty-app-v2.md`                                  | Phase M7 (re-planned) after the re-planned M6: the graphty app as HTML around the element -- the peer and the one activation import, a Settings > Performance control that writes the element's `acceleration` policy, a status-bar chip that renders `capabilities.acceleration`, gate G12                       | live plan                    |
| `warp-workgroup-barrier-defect.md`                                              | A bug report, ready to send and filed nowhere yet: the software renderer that ships with Windows returns wrong results from a compute shader that passes a value across a workgroup barrier, silently, measured on a GitHub-hosted Windows runner                                                                 | record                       |
| `dawn-indirect-dispatch-validation-cost.md`                                     | A bug report, ready to send and filed nowhere yet: under Dawn's validation every `dispatchWorkgroupsIndirect` costs about 0.4 ms of device time whether or not it dispatches anything, in Node and Chromium alike; the measurement, the reproduction, and what the graph package did about it                     | record                       |
| `../../webgpu-graph-algorithms/docs/decisions/G0.md` .. `G3.md`                 | The gate records (G1, G3 closed on the dev box; G0, G2 open until the hosted GPU runner exists)                                                                                                                                                                                                                   | records                      |
| `../../webgpu-graph-algorithms/docs/research/`                                  | The seven research notes, the three drafts, the review reports and probes the design was synthesised from                                                                                                                                                                                                         | records                      |
| `../../webgpu-graph-algorithms/docs/HEADLESS_GPU_REPORT.md`                     | The verified recipe for the real GPU under headless Chromium and Dawn-node on the dev box                                                                                                                                                                                                                         | record                       |

Path forms: the design and the contract were written in the staging repository, where the package lived at
`packages/webgpu-graph-algorithms/` and the plans at `docs/superpowers/plans/`; those forms in the bodies mean
`webgpu-graph-algorithms/` and `design/webgpu/plans/` here. The related graph-format design is
`design/graph-format/graph-format-design.md` (sections 10, 13.3, 14.3-14.6 are the ones the WebGPU design cites).
