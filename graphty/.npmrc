# Allow installation despite peer dependency conflicts
# Required because @redheadphone/react-json-grid declares peer dependency on React 18
# but actually works with React 19. This can be removed once the package updates
# its peer dependency declaration.
legacy-peer-deps=true
