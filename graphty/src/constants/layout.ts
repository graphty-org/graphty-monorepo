/**
 * Layout constants for consistent sizing across the application.
 */
import { PANEL_GRID } from "@graphty/compact-mantine";

/**
 * Width of the left sidebar (layers panel): the library's panel column.
 */
export const LEFT_SIDEBAR_WIDTH = PANEL_GRID.WIDTH;
