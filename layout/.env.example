# Development Server Configuration
# Copy this file to .env and adjust values as needed

# Server host (defaults to localhost)
# HOST=localhost
# HOST=0.0.0.0
# HOST=my.server.com

# Server port for dev server (defaults to 9010)
# PORT=9010

# Storybook port (defaults to 9011 via CLI args)
# Note: Storybook uses PORT environment variable if set

# HTTPS configuration (optional, for secure development)
# HTTPS_CERT_PATH=/path/to/cert.pem
# HTTPS_KEY_PATH=/path/to/key.pem