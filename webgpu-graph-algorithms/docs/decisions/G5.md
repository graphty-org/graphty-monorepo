# G5 -- Fruchterman-Reingold and the spring-electrical preset (spec 13 row P5)

Recorded by: Task P5-T11 of `design/webgpu/plans/2026-09-20-webgpu-p5-fruchterman-reingold.md`, 2026-09-20 (every
measurement below ran on the dev box on 2026-09-20 local time; the benchmark session date is UTC). The owner signs
section 7. Commits: the twelve of the P5 PR (`{{OPEN: short hashes once committed}}`): the eleven task commits plus the
lane follow-up commit that carries the T4 baseline (plan 8.3).
Environment: `@graphty/webgpu-graph-algorithms` 0.5.0 (0.4.1 when the plan was written; `5ba15bb7 chore(release):
publish` landed between) over `@graphty/layout` 1.7.0, Node v22.22.1, pnpm 10.0.0, vitest 3.2.7, the `webgpu` npm
package 0.4.0 (Dawn), Ubuntu 22.04.5 LTS, NVIDIA driver 580.173.02, Mesa lavapipe 23.2.1-1ubuntu3.1~22.04.3 (LLVM
15.0.7), Playwright 1.57.0 with Chromium 143.0.7499.4 (headless shell 1200), `ngraph.forcelayout` 3.3.1 and
`ngraph.graph` 20.1.1 (test tree only). Every command ran from `webgpu-graph-algorithms/` in the worktree
`.worktrees/gpu-scale-layouts` (branch `feat/gpu-p5-p4`, based on `c6f1e85f` plus the `0.5.0` publish commit
`5ba15bb7`); the logs are under `tmp/p5/t11/` (gitignored): `check.sh` runs the eight legs of the plan's Step 1 in
order and writes `<leg>.log` / `<leg>.exit`, `maxima.mjs` folds a node log into the per-stage maxima quoted below.

The rule of this record (spec 10.4): every number is measured and names the command or the file it came from; a
missed target is never relaxed here -- it is re-fixed by an owner decision in section 7, or the phase stays open.

GATE STATUS: GREEN on the dev box (every item of section 2 passes on NVIDIA-Dawn, on lavapipe and in Chromium on
SwiftShader and on the RTX 4070 SUPER); the GPU lane (`gpu-linux-t4`) has not run this branch (rule (a) of spec 13
wants both lanes), so the rows of sections 1, 3 and 6 that need it carry an `{{OPEN: ...}}` marker with the command
that fills them, exactly as G7 did before its lane run.

## 0. What the phase built (the deliverable of spec 13 row P5)

`createFruchtermanReingold` and `createSpringElectrical` on the exact tier over the P3 kernels (the `LAW`, `APPLY`
and `STATS_MODE` override branches of K2 / K3, K5 and K1; no new kernel id, no new binding), the two stats records
(`FruchtermanReingoldStats` with the `temperature` trace, `SpringElectricalStats` with the `kineticEnergy` trace),
the two f64 oracles with f32 variants (the second checked against `ngraph.forcelayout` 3.3.1 itself), the FR `fixed`
option applied at load through `ModelInputs.fixed`, the accelerator members `fruchtermanReingold` / `springElectrical`,
the `layout-fr` benchmark group with T-14, 25 sabotage rows in `SABOTAGE_P5`, 44 noise-floor rows and 46 derived
tolerances over 158 fixture files (42 members), the property, force-sum and distributional suites of both models, the
FR frame loop and the browser smoke `test/browser/spring-layouts.test.ts`.
The three departures from the design are `design/decisions/2026-09-20-spring-electrical-settles-by-the-shared-rule.md`,
`design/decisions/2026-09-20-spring-electrical-integrates-like-ngraph.md` and
`design/decisions/2026-09-20-fr-reheat-restarts-the-temperature-not-the-budget.md`.

## 1. Adapters exercised

| Adapter | Runtime | adapter class | runner class | subgroups | how it was run |
| --- | --- | --- | --- | --- | --- |
| NVIDIA GeForce RTX 4070 SUPER, Dawn (driver 580.173.02) | node | nvidia-lovelace-node | nvidia-lovelace-driver580 | 32 / 32 | `LD_LIBRARY_PATH=<the libEGL tree> GRAPHTY_GPU_REQUIRE=hardware pnpm exec vitest run --project=node`: 93 files, 1834 passed, 1 skipped of 1835, 92.21 s (`node-nvidia.log`); the twin pass `GRAPHTY_GPU_NO_SUBGROUPS=1 ... test/primitives test/layouts test/algorithms`: 43 files, 731 passed, 16 skipped of 747, 87.38 s (`node-nvidia-nosub.log`) |
| Mesa lavapipe (Mesa 23.2.1, LLVM 15.0.7), Dawn | node | mesa-software-node | -- (a software adapter is never timed) | 8 / 8 | the default-lane env `GRAPHTY_GPU_ADAPTER=llvmpipe GRAPHTY_GPU_REQUIRE=any VK_DRIVER_FILES=/usr/share/vulkan/icd.d/lvp_icd.x86_64.json XDG_RUNTIME_DIR=/tmp pnpm exec vitest run --project=node --coverage`: 93 files, 1834 passed, 1 skipped of 1835, 239.32 s (241 s wall by bash's `time`), coverage in section 5 (`node-lavapipe-cov.log`); the process exited 1 on ONE unhandled vitest-worker RPC timeout with every test green (finding G5-F2, which also records the rerun) |
| Chromium 143 on SwiftShader | browser | google-swiftshader-browser | -- | 4 / 4 | `GRAPHTY_BROWSER_GPU=swiftshader GRAPHTY_GPU_REQUIRE=any node scripts/run-browser-project.js`: 11 files passed, 1 skipped (`bench.test.ts`, GPU lane only), 52 passed, 1 skipped of 53, 48.71 s; the compile matrix (`compile-matrix.test.ts`, 20 cases, every override case of the registry after PD-9) 22.894 s (`browser-swiftshader.log`) |
| Chromium 143 on the RTX 4070 SUPER | browser | nvidia-lovelace-browser | -- | 32 / 32 | `LD_LIBRARY_PATH=<the libEGL tree> GRAPHTY_BROWSER_GPU=nvidia GRAPHTY_GPU_REQUIRE=nvidia node scripts/run-browser-project.js` (`isFallbackAdapter=false`): 12 files, 53 passed of 53 including `bench.test.ts`, 20.43 s; the compile matrix 0.767 s (`browser-nvidia.log`) |
| GitHub gpu-linux-t4 (NVIDIA Tesla T4, a machine.dev runner) | node + browser | nvidia-turing-node / nvidia-turing-browser | gpu-linux-t4 | 32 / 32 | `{{OPEN: push the branch, open the PR with the gpu label so gpu.yml runs its node, node-limits, twin, browser and bench steps; record the run id here}}` |

The libEGL tree is `/home/apowers/Projects/graphty-monorepo/tmp/egl/root/usr/lib/x86_64-linux-gnu` (`CLAUDE.md`,
"Running the suites locally"); without it Dawn lists no hardware adapter and `GRAPHTY_GPU_REQUIRE=hardware` fails up
front in the global setup with `E_SOFTWARE_ONLY`. `pnpm run build:all && pnpm run lint` green (eslint plus the two
tsc runs, `build-lint.log`, 13 s); root `pnpm exec knip` after the fixes of finding G5-F3 reports nothing under this
package beyond the pre-existing G7-F4 finding (`benchmarks/run.ts` and its two `harness.ts` imports); `LC_ALL=C grep
-rnP '[^\x00-\x7F]' src test benchmarks docs | wc -l` = 0 (finding G5-F7 on the one binary fixture grep names).

## 2. The G5 checklist (spec 13 row P5), each item mapped to its evidence

The per-item maxima below are the figures the suites themselves print (`console.warn` lines tagged `[fr-inspect]`,
`[se-inspect]`, `[fr-twins]`, `[se-twins]`, `[fr-layout-oracle]`, `[fr-trace]`, `[se-trace]`, `[fr-distributional]`,
`[se-distributional]`, `[fr-force-sum]`, `[se-force-sum]`, `[sabotage]` and `[se-settle]`), folded per stage by
`tmp/p5/t11/maxima.mjs` over `node-nvidia.log` and `node-lavapipe-cov.log`. "NVIDIA / lavapipe" pairs are in that
order. The lavapipe figures are at `gpuScale()` sizes (the random1k cases are scaled down on a software adapter), so
they are not the same fixtures as the NVIDIA ones where the graph is named `random1k`. The suites assert the bounds;
the printed lines only report the maxima.

| # | Item | Evidence | Result | Status |
| --- | --- | --- | --- | --- |
| 1 | one-iteration displacement parity with the CPU FR oracle (<= 1e-4) | `test/layouts/fr-inspect.test.ts` (`displacement` key; 23 cases over karate, path10, grid10, star200, random1k in 2D and 3D at `k: 0.3` and `k: auto`, plus the noise re-checks), tolerance `fr-displacement` = 1.000e-4 (the design's cap; basis `fr-displacement.oracle-f64` = 1.991e-5, 10x would exceed the cap so the factor is 5.02) | max error over the cases 1.854e-5 (random1k/2d/k=0.3) / 6.050e-7 (path10/2d/k=auto); the noise fixture `fr-random1k-k03-K5-disp` 1.991e-5 vs the f64 oracle; every case run twice bitwise | pass |
| 2 | fixed nodes immobile | `fr-inspect.test.ts` pinned case (the masked rows' displacement is 0 exactly), `fr-behaviour.test.ts` case 4 (`fixed` at creation: the pinned row bitwise where the seed put it after `run({ maxIter: 20 })`, by mask, by column name and by the `fixed` role), `fr-layout-oracle.test.ts` karate/fixed (the pinned rows bitwise, the free rows within `fr-layout-oracle`), `fr-properties.test.ts` (the fixed-nodes property, 200 runs), `test/browser/spring-layouts.test.ts` (`setFixed` honoured on both browsers); the sabotage row `fr-fixed-moves` fails at ratio Infinity (a pinned node moved 0.1) | bitwise on every adapter | pass |
| 3 | output NOT rescaled when `fixed` is given | `fr-behaviour.test.ts` case 4 (`scale: 3, center: [10, 20, 0]`: every free row seeded in x [10, 13), y [20, 23) moves at most the temperature 0.1 per iteration, so after 20 iterations it is asserted inside the analytic box x [4, 19], y [14, 29], z === 0 -- a rescale to [-1, 1] would miss it on every row); `fr-layout-oracle.test.ts` karate/scale3-center against `@graphty/layout`'s CPU class writing `layout * scale + center` (tolerance `fr-layout-oracle` = 1.813e-4) | the box holds on both adapters; the layout-oracle error at k = 10 (asserted) 2.619e-7 / 6.318e-7, at k = 20 (printed, informational: karate/fixed is not admitted at 20, its f64 spread 2.861e-4 is above a third of the cap) 6.314e-6 / 2.294e-5 | pass |
| 4 | the preset settles within 1,000 steps on the 150 / 250 story graph to an edge-length distribution within 25% of ngraph's | `test/layouts/se-settle.test.ts` (both adapters; the story graph deduped to 249 unordered pairs, DEP-P5-A: the shared 7.17 rule stops the GPU, ngraph's own `step()` return stops ngraph), `test/browser/spring-layouts.test.ts` case 5 | NVIDIA: GPU stop 496 iterations (settled true), ngraph stop 346 steps (stable true); q10 / q50 / q90 GPU 10.9328 / 15.0074 / 20.9371 vs ngraph 10.8650 / 15.3043 / 20.7020, worst 1.94% (q50). lavapipe: GPU stop 480, ngraph 346; 11.4680 / 15.1723 / 21.1782 vs the same ngraph figures, worst 5.55% (q10). Kinetic energy 158 on the first batch, 0.992 / 0.286 at the stop. Chromium: 480 iterations on SwiftShader, 496 on the RTX 4070 SUPER | pass |
| 5 | T-14 recorded | section 3 | 0.617 ms at 10k, 16.367 ms at 100k (`fr ms/iteration (profiler)`, medians of 5) | pass |
| 6 | rule (f): sabotage rows per kernel branch, each >= 10x | `test/sabotage/fr.test.ts` (16 cases), `test/sabotage/se.test.ts` (15 cases) over `SABOTAGE_P5` (`test/helpers/sabotage.ts`): K2 6 rows (3 FR, 3 spring), K3 6 (3 FR, 3 Coulomb), K5 7 (3 FR, 4 Euler), K1 6 (3 FR temperature, 3 kinetic energy) | 25 rows, every one >= 10x on both adapters; smallest ratio 1.308e+3 (`fa2-integrate/euler-mass-ignored`, positions after 5 iterations vs the f64 oracle; through the one-iteration stage check this mutant is bitwise the pristine kernel, so its row is a trace check -- P5-T7's note), then `euler-drag-sign` 1.335e+3, `euler-velocity-not-stored` 1.623e+3, `fr-repulsion-law-skipped` 3.920e+3; five rows Infinity (`fr-fixed-moves`, `fr-temperature-not-traced`, `fr-stats-mode-skipped`, and the two temperature / kinetic-energy trace pins); the unmutated baselines measure 5.338e-3 .. 1.000e-1 (FR) and 2.950e-3 .. 2.236e-1 (spring), i.e. under 1 | pass |
| 7 | rule (f): `inspect()` stage comparisons of every branch | `fr-inspect.test.ts` (7 stage keys: attraction, force, partials, displacement, positions, scene, k1), `se-inspect.test.ts` (8: the same plus velocity), each key against the f64 oracle's stage under its own `fr-inspect.*` / `se-inspect.*` tolerance, the whole set twice bitwise | worst stage error FR 2.150e-5 (force, random1k/2d/k=0.3) / 6.368e-7 (force, karate/2d/k=auto); spring 1.803e-5 (positions and scene, random1k/2d) / 4.116e-6 (k1, karate/2d); the worst ratio to a tolerance 2.197e-1 (FR k1 on lavapipe), every other ratio under 2.2e-1 | pass |
| 8 | rule (f): a noise-floor row and a derived tolerance per tolerance | section 4; `benchmarks/results/noise-floor.json` tolerances `fr-*` / `se-*` (recorded 2026-09-20T20:20:51.368Z by Task P5-T4's sequence: the writer files under `GRAPHTY_NOISE_FLOOR_WRITE=1` on NVIDIA, then on lavapipe, then `test/noise-floor.test.ts` in write mode on both, then `prettier --write`) | 46 tolerances (23 per model, ten of them `.cross`) over 44 rows; every tolerance is 10x its basis row or the design's cap, whichever is smaller: eleven are clipped at the cap with a factor under 10 (finding G5-F5), none is above its cap, and every measured value of section 2 is under its tolerance | pass |
| 9 | the oracle cross-checked against an independent implementation (11.4) | `test/oracle/spring-electrical-ngraph.test.ts` (5 cases, no device): the f64 oracle vs `ngraph.forcelayout` run with `theta: 0` from the SAME positions on karate after 1 and 5 steps and on the 150 / 249 story graph after 1 and 5 steps, every position and the kinetic energy within 1e-9 relative (floored at 1e-12 absolute), plus ngraph's own numbers pinned on a three-node path and a pinned body on both sides; `fr-layout-oracle.test.ts` (10 cases) vs `@graphty/layout`'s `FruchtermanReingoldSimulation` at the admitted horizons | ngraph: the analytic 1e-9 bound holds (asserted, no noise floor); `@graphty/layout`: noise member karate/fixed at horizon 10 error 4.934e-6 / 1.813e-5 under `fr-layout-oracle` 1.813e-4, every admitted horizon of grid10, karate and random1k at ratios 5.5e-4 .. 4.0e-1 of the tolerance | pass |
| 10 | run twice bitwise; subgroup twins in-process; the no-subgroups pass | every `fr-*` / `se-*` suite asserts `expectBitwiseEqual` on its second run first; `fr-twins.test.ts` (4 cases) and the spring twins inside `se-inspect.test.ts` compare the subgroup and no-subgroup contexts in-process; Step 1's `GRAPHTY_GPU_NO_SUBGROUPS=1` pass over `test/primitives test/layouts test/algorithms` | twins: attraction, force, displacement, positions, scene and velocity bitwise (0.000e+0) on both models; the reductions (partials, k1) FR worst rel 1.872e-7 (random1k/k1) / 1.980e-7 (random1k/partials), spring worst rel 5.703e-6 (karate/k1) / 2.816e-6 (karate/partials), under `fr-twins.*` / `se-twins.*`; the twin pass 731 passed, 16 skipped (the cases that need the feature the pass disables) | pass |
| 11 | lifecycle: leak 0, `E_RELEASED`, device loss | `fr-lifecycle.test.ts` (4 cases): `dispose()` leaves no live buffer and a `step()` maps at most two staging slots; `E_NOT_LOADED` before `load()` and `E_UNSUPPORTED { feature: "repulsion.grid" }` for the grid tier; `release(snapshot)` during a live simulation makes the next `step()` `E_RELEASED`; device loss mid-run rejects the pending step `E_DEVICE_LOST`, disposes the simulation and a fresh context runs afterwards. The spring preset's lifecycle rides `se-behaviour.test.ts` and the browser smoke (`pool.liveBytes` 0 after dispose) | 0 live buffers on both adapters | pass |
| 12 | browser smoke on SwiftShader and NVIDIA-Chromium | `test/browser/spring-layouts.test.ts` (5 cases: the FR and spring 500-node smokes, the FR frame loop on random1k over 600 ticks, `setPosition` during flight, the story-graph settle) | SwiftShader: `GRAPHTY_BROWSER_GPU=swiftshader GRAPHTY_GPU_REQUIRE=any node scripts/run-browser-project.js` -> 11 files passed, 1 skipped, 52 passed, 1 skipped of 53, 48.71 s, the file 10.412 s; NVIDIA: `... GRAPHTY_BROWSER_GPU=nvidia GRAPHTY_GPU_REQUIRE=nvidia node scripts/run-browser-project.js` -> 12 files, 53 passed of 53, 20.43 s, the file 5.765 s | pass |
| 13 | FA2 unchanged: every committed FA2 noise fixture still matches | `fa2-inspect.test.ts` and `fa2-twins.test.ts` (28 cases together) in both whole-project runs; Task P5-T2 Step 4 asserted the FA2 suite bitwise against its committed noise fixtures after the `LAW = 0` / `APPLY = 0` / `STATS_MODE = 0` paths gained their extra reduction and accumulator | bitwise on NVIDIA and lavapipe; no P1 / P3 sabotage row, FA2 test or FA2 tolerance changed (plan 0.7) | pass |
| 14 | the lavapipe lane budget (T-12) | Step 1's timed run: `time eval $GPU_LLVM pnpm exec vitest run --project=node --coverage` | 241 s of 900 at the default fork count (vitest's own `Duration` 239.32 s; the process exit 1 of finding G5-F2 is a worker RPC timeout, not a test); 298 s, exit 0 at the CI-like `--maxWorkers=3`; 59 s without `--coverage` | pass |
| 15 | `CLAUDE.md` step 6: the fast-check property rows of both models | `test/layouts/fr-properties.test.ts` (7 rows), `se-properties.test.ts` (8 rows; the plan said 7, P5-T5 added the run-level `maxIter` cap as its own row), `numRuns` 200 on every adapter | summed case time on lavapipe 61.0 s (FR) and 43.1 s (spring), on NVIDIA 8.5 s and 8.1 s (the verbose reporter's per-case times); standalone wall time per file on lavapipe 46 s (FR: vitest `Duration` 44.95 s) and 31 s (spring: 30.34 s), by `pnpm exec vitest run --project=node test/layouts/<file>` under the default-lane env (`props-fr-lavapipe.log`, `props-se-lavapipe.log`) | pass |
| 16 | `CLAUDE.md` step 6: the force-sum invariant (every P5 law is antisymmetric) | `fr-force-sum.test.ts`, `se-force-sum.test.ts` (7 cases each: karate, path10, grid10, star200, random1k and the coincident fixture after one iteration with the centre gravity 0); tolerances `fr-force-sum` = 1.000e-4 (basis `fr-force-parity.oracle-f64`), `se-force-sum` = 6.299e-5 (basis `se-force-parity.oracle-f64`) | `\|sum F\| / sum \|F\|` worst FR 7.338e-8 (star200, ratio 7.338e-4 of the tolerance) / 3.155e-8 (ratio 3.155e-4); spring 8.112e-8 (random1k, ratio 1.288e-3) / 3.116e-8 (the coincident fixture, ratio 4.947e-4) | pass |
| 17 | `CLAUDE.md` step 6: distributional parity after 100 iterations | `fr-distributional.test.ts`, `se-distributional.test.ts` (`layoutMetrics` of the GPU layout vs the f64 oracle's, coordinates never compared); tolerances `fr-distributional` = 0.1 (the cap; basis 2.200e-2, factor 4.55), `se-distributional` = 1.267e-2 (10x its basis 1.267e-3); a candidate is admitted only where the oracle reproduces its own metrics within a third of the cap (3.333e-2) under eight one-ulp start perturbations | FR admitted karate/3d (oracle spread 3.705e-3) and random1k/3d (1.600e-2); left out karate/2d 8.824e-2, random1k/2d 8.000e-2, star200/2d 4.975e-2; worst error 2.200e-2 (random1k/3d) / 3.257e-3 (karate/3d). Spring admitted karate/2d (2.941e-2), karate/3d (2.302e-6), random1k/3d (1.241e-5); left out random1k/2d 3.244e-1, star200/2d 4.346e-2; worst error 1.267e-3 (karate/2d, the recording adapter's own floor) / 8.316e-4 | pass |

## 3. T-14 (spec 10.4; `benchmarks/results/nvidia-lovelace-driver580.json`, session 2026-09-20T19:25:37.311Z)

The dev box (RTX 4070 SUPER, driver 580.173.02), the `layout-fr` group of Task P5-T8: one `createFruchtermanReingold`
and one `createSpringElectrical` simulation per rung 10k / 100k (E = 10n, 2D, `repulsion: "exact"`), the same
untimed clock warm-up burst and `reheat()` + `step(1)` protocol as `layout-exact`, medians of 5; the `ms/iteration
(profiler)` rows are `stats.msPerIteration` (the GPU time of the passes, `timestamp-query` granted), the `step(1)
wall` rows add `toScene` and the 12n readback. The session is the file's fourth and last, so it is the baseline
`bench:compare` reads; its `layout-exact` T-4 row measured 0.588 ms at 10k in the same session, so FR at 10k is
1.05x FA2 and the spring preset at 100k is 1.11x FR (P5-T8's note: both recorded as measured).

| Id | Benchmark (group / name) | Target | Measured median of 5 (ms) | min / max (ms) | Rate | Pass |
| --- | --- | --- | --- | --- | --- | --- |
| T-14 | layout-fr / fr ms/iteration (profiler) n=10000 [10k] | recorded | 0.617 | 0.616 / 0.618 | 1.619e+11 pairs/s | recorded |
| T-14 | layout-fr / fr ms/iteration (profiler) n=100000 [100k] | recorded | 16.367 | 16.312 / 16.394 | 6.110e+11 pairs/s | recorded |
| -- | layout-fr / se ms/iteration (profiler) n=10000 [10k] | recorded | 0.648 | 0.646 / 0.648 | 1.543e+11 pairs/s | recorded |
| -- | layout-fr / se ms/iteration (profiler) n=100000 [100k] | recorded | 18.154 | 18.058 / 18.173 | 5.508e+11 pairs/s | recorded |
| -- | layout-fr / fr step(1) wall n=10000 m=100000 2D [10k] | -- | 0.786 | 0.770 / 1.752 | 1.272e+11 pairs/s | -- |
| -- | layout-fr / fr step(1) wall n=100000 m=1000000 2D [100k] | -- | 17.044 | 16.973 / 29.034 | 5.867e+11 pairs/s | -- |
| -- | layout-fr / se step(1) wall n=10000 m=100000 2D [10k] | -- | 0.810 | 0.797 / 0.843 | 1.235e+11 pairs/s | -- |
| -- | layout-fr / se step(1) wall n=100000 m=1000000 2D [100k] | -- | 18.698 | 18.607 / 22.345 | 5.348e+11 pairs/s | -- |

The same four `ms/iteration` rows for `gpu-linux-t4` once the lane has run: `{{OPEN: P5-T8 Step 3}}`. Owner procedure:
(1) push `feat/gpu-p5-p4` and open the PR with the `gpu` label so `gpu.yml` runs; its `pnpm run bench` and
`bench-compare` steps report the eight `layout-fr` rows as `new (no baseline)` and exit 0 (a FAILED compare is a
finding about another group, read first); (2) `gh run download <run id> -n gpu-results-<run id> -D tmp/p5/t4` (expect
`benchmarks/out/gpu-linux-t4.json` among the files); (3) from `webgpu-graph-algorithms/`, `node
tmp/p5/append-session.mjs tmp/p5/t4/benchmarks/out/gpu-linux-t4.json benchmarks/results/gpu-linux-t4.json`; (4) copy
the T4 `fr ms/iteration (profiler)` medians at 10k and 100k into `README.md`'s T4 table (its T-14 row carries the
`{{OPEN: ...}}` marker), into the table above and into `CLAUDE.md`'s "Settled at G5" row; commit as `feat(webgpu-graph-algorithms):
record the layout-fr T4 lane baseline`.

## 4. Cross-adapter results (spec 11.5, 11.9; `benchmarks/results/noise-floor.json`)

The 44 P5 rows (22 per model), recorded by Task P5-T4's one recording run on both adapters (2026-09-20T20:20:51.368Z):
per kernel branch an `oracle-f64` row (the adapter the run found worse against the f64 oracle is the one recorded)
and a `cross-adapter` row (lavapipe against NVIDIA), plus the two `twin` rows of each model (the no-subgroups
context against the subgroup context on the reductions). The fixtures are
`test/fixtures/noise/fa2-<kernel>-<member>-{nvidia-lovelace-node,mesa-software-node,oracle-f64}.json` (158 files over
42 members, the `fr-` and `se-` members; the K1, K3, K5 and K5-partials members of karate and random1k of both models also carry a `-no-subgroups` twin file per adapter, 32 of the 158). A
`fixture` column names the member: `fr-random1k-k03-*` is random1k at `k: 0.3` (the worse-conditioned FR case), `fr-grid10-traj10` is the 10-iteration trajectory
member (grid10, because free karate is not admitted at horizon 10: its f32 oracle sits 3.728e-4 from the f64, above a
third of the cap), `fr-karate-layout10` is karate WITH the fixed mask at horizon 10 (the same rule), `se-karate-*` the
spring members where random1k is not admitted (the f64 oracle's own one-ulp sensitivity on it is 1.015e+0 at horizon 5),
and the `-metrics100` members are the distributional cases (17 / 16 metric keys as samples). Two absolute columns
need a word: `se-force-parity.oracle-f64` reports maxAbsError 3.887e+1 because ngraph's `gravity -12` Coulomb forces
on the unit-square start are tens to hundreds per node (its relative error is 6.299e-6), and `se-force-parity.cross`
2.000e+0 for the same reason at 1.599e-6 relative.

| Row id | kernel / fixture | comparison | a | b | maxRelError | maxAbsError | samples |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `fr-inspect.attraction.cross` | fa2-attraction / fr-random1k-K2 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 5.912e-7 | 6.104e-5 | 3000 |
| `fr-inspect.attraction.oracle-f64` | fa2-attraction / fr-random1k-k03-K2 | oracle-f64 | mesa-software-node | oracle-f64 | 6.348e-7 | 9.186e-6 | 3000 |
| `fr-force-parity.cross` | fa2-repulsion-exact / fr-random1k-k03-K3 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 4.131e-6 | 1.907e-5 | 3000 |
| `fr-force-parity.oracle-f64` | fa2-repulsion-exact / fr-random1k-k03-K3 | oracle-f64 | mesa-software-node | oracle-f64 | 2.388e-5 | 1.012e-4 | 3000 |
| `fr-twins.force.twin` | fa2-repulsion-exact / fr-random1k-K3 | twin | mesa-software-node | mesa-software-node/no-subgroups | 0.000e+0 | 0.000e+0 | 3000 |
| `fr-displacement.cross` | fa2-integrate / fr-random1k-k03-K5-disp | cross-adapter | mesa-software-node | nvidia-lovelace-node | 3.372e-6 | 2.980e-7 | 3000 |
| `fr-displacement.oracle-f64` | fa2-integrate / fr-random1k-k03-K5-disp | oracle-f64 | mesa-software-node | oracle-f64 | 1.991e-5 | 1.502e-6 | 3000 |
| `fr-distributional.cross` | fa2-integrate / fr-random1k-3d-metrics100 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 2.300e-2 | 1.700e-2 | 17 |
| `fr-distributional.oracle-f64` | fa2-integrate / fr-random1k-3d-metrics100 | oracle-f64 | nvidia-lovelace-node | oracle-f64 | 2.200e-2 | 1.300e-2 | 17 |
| `fr-inspect.partials.cross` | fa2-integrate / fr-random1k-k03-K5-partials | cross-adapter | mesa-software-node | nvidia-lovelace-node | 3.433e-7 | 3.433e-5 | 13 |
| `fr-inspect.partials.oracle-f64` | fa2-integrate / fr-random1k-K5-partials | oracle-f64 | mesa-software-node | oracle-f64 | 2.670e-7 | 2.670e-5 | 13 |
| `fr-inspect.positions.cross` | fa2-integrate / fr-random1k-K5 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 1.218e-6 | 5.960e-8 | 3000 |
| `fr-inspect.positions.oracle-f64` | fa2-integrate / fr-random1k-k03-K5 | oracle-f64 | mesa-software-node | oracle-f64 | 4.208e-6 | 1.502e-6 | 3000 |
| `fr-layout-oracle.cross` | fa2-integrate / fr-karate-layout10 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 1.596e-5 | 5.692e-6 | 102 |
| `fr-layout-oracle.oracle-f64` | fa2-integrate / fr-karate-layout10 | oracle-f64 | mesa-software-node | oracle-f64 | 1.813e-5 | 3.934e-6 | 102 |
| `fr-trajectory.cross` | fa2-integrate / fr-grid10-traj10 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 1.958e-4 | 1.514e-5 | 300 |
| `fr-trajectory.oracle-f64` | fa2-integrate / fr-grid10-traj10 | oracle-f64 | mesa-software-node | oracle-f64 | 1.817e-4 | 1.978e-5 | 300 |
| `fr-twins.positions.twin` | fa2-integrate / fr-random1k-K5-partials | twin | mesa-software-node | mesa-software-node/no-subgroups | 3.433e-7 | 3.433e-5 | 13 |
| `fr-inspect.scene.cross` | fa2-to-scene / fr-random1k-toScene | cross-adapter | mesa-software-node | nvidia-lovelace-node | 1.218e-6 | 5.960e-8 | 3000 |
| `fr-inspect.scene.oracle-f64` | fa2-to-scene / fr-random1k-k03-toScene | oracle-f64 | mesa-software-node | oracle-f64 | 4.208e-6 | 1.502e-6 | 3000 |
| `fr-inspect.k1.cross` | fa2-stats-finalize / fr-random1k-K1 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 2.980e-7 | 2.980e-8 | 9 |
| `fr-inspect.k1.oracle-f64` | fa2-stats-finalize / fr-random1k-K1 | oracle-f64 | mesa-software-node | oracle-f64 | 2.086e-7 | 4.538e-8 | 9 |
| `se-inspect.attraction.cross` | fa2-attraction / se-random1k-K2 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 6.590e-7 | 7.629e-6 | 3000 |
| `se-inspect.attraction.oracle-f64` | fa2-attraction / se-random1k-K2 | oracle-f64 | nvidia-lovelace-node | oracle-f64 | 9.843e-7 | 7.341e-6 | 3000 |
| `se-force-parity.cross` | fa2-repulsion-exact / se-random1k-K3 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 1.599e-6 | 2.000e+0 | 3000 |
| `se-force-parity.oracle-f64` | fa2-repulsion-exact / se-random1k-K3 | oracle-f64 | mesa-software-node | oracle-f64 | 6.299e-6 | 3.887e+1 | 3000 |
| `se-twins.force.twin` | fa2-repulsion-exact / se-random1k-K3 | twin | mesa-software-node | mesa-software-node/no-subgroups | 0.000e+0 | 0.000e+0 | 3000 |
| `se-displacement.cross` | fa2-integrate / se-random1k-K5-disp | cross-adapter | mesa-software-node | nvidia-lovelace-node | 3.076e-6 | 1.252e-6 | 3000 |
| `se-displacement.oracle-f64` | fa2-integrate / se-random1k-K5-disp | oracle-f64 | mesa-software-node | oracle-f64 | 9.762e-6 | 4.195e-6 | 3000 |
| `se-distributional.cross` | fa2-integrate / se-karate-metrics100 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 2.096e-3 | 8.115e-3 | 16 |
| `se-distributional.oracle-f64` | fa2-integrate / se-karate-metrics100 | oracle-f64 | nvidia-lovelace-node | oracle-f64 | 1.267e-3 | 4.899e-3 | 16 |
| `se-inspect.partials.cross` | fa2-integrate / se-karate-K5-partials | cross-adapter | mesa-software-node | nvidia-lovelace-node | 8.447e-6 | 4.768e-7 | 14 |
| `se-inspect.partials.oracle-f64` | fa2-integrate / se-karate-K5-partials | oracle-f64 | nvidia-lovelace-node | oracle-f64 | 4.348e-6 | 2.258e-6 | 14 |
| `se-inspect.positions.cross` | fa2-integrate / se-random1k-K5 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 1.129e-5 | 1.252e-6 | 3000 |
| `se-inspect.positions.oracle-f64` | fa2-integrate / se-random1k-K5 | oracle-f64 | mesa-software-node | oracle-f64 | 2.632e-5 | 4.195e-6 | 3000 |
| `se-inspect.velocity.cross` | fa2-integrate / se-random1k-K5-velocity | cross-adapter | mesa-software-node | nvidia-lovelace-node | 3.076e-6 | 2.503e-6 | 3000 |
| `se-inspect.velocity.oracle-f64` | fa2-integrate / se-random1k-K5-velocity | oracle-f64 | mesa-software-node | oracle-f64 | 9.786e-6 | 8.331e-6 | 3000 |
| `se-trajectory.cross` | fa2-integrate / se-karate-traj10 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 2.947e-5 | 8.035e-5 | 102 |
| `se-trajectory.oracle-f64` | fa2-integrate / se-karate-traj10 | oracle-f64 | mesa-software-node | oracle-f64 | 5.279e-5 | 1.128e-4 | 102 |
| `se-inspect.scene.cross` | fa2-to-scene / se-random1k-toScene | cross-adapter | mesa-software-node | nvidia-lovelace-node | 1.129e-5 | 1.252e-6 | 3000 |
| `se-inspect.scene.oracle-f64` | fa2-to-scene / se-random1k-toScene | oracle-f64 | mesa-software-node | oracle-f64 | 2.632e-5 | 4.195e-6 | 3000 |
| `se-inspect.k1.cross` | fa2-stats-finalize / se-karate-K1 | cross-adapter | mesa-software-node | nvidia-lovelace-node | 8.507e-6 | 1.490e-8 | 9 |
| `se-inspect.k1.oracle-f64` | fa2-stats-finalize / se-karate-K1 | oracle-f64 | nvidia-lovelace-node | oracle-f64 | 4.392e-6 | 9.356e-8 | 9 |
| `se-twins.positions.twin` | fa2-stats-finalize / se-karate-K1 | twin | nvidia-lovelace-node | nvidia-lovelace-node/no-subgroups | 5.703e-6 | 1.192e-7 | 9 |

The committed state was re-verified without the write flag on both adapters by Step 1's whole-project runs
(`test/noise-floor.test.ts` 66 cases green on each, the `fr-*` / `se-*` suites re-reading every fixture). No browser
row: as for `spmv-pull` (G7-F3), the browser project has no FR / spring noise writer -- the compile matrix writes
`segmented-reduce` only -- so the `google-swiftshader-browser` and `nvidia-lovelace-browser` classes of the P1-P3 rows
do not exist for these members (finding G5-F6).

## 5. Coverage (spec 11.8; the default-lane run of Step 1)

| lines | functions | branches | statements | threshold | wall time |
| --- | --- | --- | --- | --- | --- |
| 95.38 % | 98.82 % | 94.70 % | 95.38 % | 80 / 80 / 75 / 80 (`vitest.config.ts`, the `node` project) | 241 s (lavapipe, `--coverage`, `node-lavapipe-cov.log`; vitest's `Duration` 239.32 s), 298 s at `--maxWorkers=3` with the same percentages; 59 s on lavapipe and 92.21 s on NVIDIA without coverage |

`src/layouts` 93.09 % lines (the three model files and `model-common.ts` included), `src/kernel` 95.33 %,
`src/types` 0 % as always (declaration-only files). The thresholds are unchanged.

## 6. Baselines committed

- `benchmarks/results/nvidia-lovelace-driver580.json`: a fourth session (2026-09-20T19:25:37.311Z, driver
  580.173.02) carrying every group -- `upload`, `roundtrip`, `layout-exact`, `pagerank`, `wcc` and the new
  `layout-fr` (eight rows); it is the file's last session and therefore the baseline `bench:compare` reads (Task
  P5-T8; `tmp/p5/append-session.mjs`).
- `benchmarks/results/gpu-linux-t4.json`: UNCHANGED; `{{OPEN: P5-T8 Step 3 appends the lane session that carries
  layout-fr (section 3)}}`.
- `benchmarks/results/noise-floor.json`: 44 new rows and 46 new tolerances (section 4; `recordedAt`
  2026-09-20T20:20:51.368Z); no P1 / P3 / P7 row or tolerance changed.
- `test/fixtures/noise/fa2-*-fr-*.json`, `fa2-*-se-*.json`: 158 new files (42 members).
- `README.md`: the T-14 row of the dev-box table (0.617 ms; 16.367 ms), the `layout-fr` group in the group list, the
  sentence naming the T-14 session, and the T4 table's T-14 row with its `{{OPEN: ...}}` marker (Task P5-T8).
- `CLAUDE.md` (this package): the inventory lines, the `layout-fr` benchmark row, the "Adding a Layout Model"
  preamble and the "Settled at G5" table (Task P5-T11 Step 3).

## 7. Findings, owner decisions, re-fixed targets

| Id | Finding | Owner decision |
| --- | --- | --- |
| G5-F1 | The GPU lane (`gpu-linux-t4`) has not run this branch: rule (a) of spec 13 wants the gate green on the default lane AND the GPU lane. Section 1's last row, section 3's T4 rows, section 6's `gpu-linux-t4.json` bullet and the README's T4 T-14 row carry `{{OPEN: ...}}` with the procedure (section 3). The lane's `bench-compare` will report the eight `layout-fr` rows `new (no baseline)`, which its rule 3 passes. | `{{OPEN: owner -- label the PR gpu, then fill the rows}}` |
| G5-F2 | The lavapipe `--coverage` run exited 1 with every test green (93 files, 1834 passed, 1 skipped): vitest reported ONE unhandled error, `[vitest-worker]: Timeout calling "onTaskUpdate"` (the worker-to-main RPC timed out while the main thread was busy), after `test/layouts/se-distributional.test.ts` finished. No test failed and no `expect` was involved; the same tree on NVIDIA and the twin pass exited 0. The run's wall time was 241 s against 66.5 s for the 71-file pre-P5 tree at the P5 review run (`tmp/rev-llvm-full-cov.log`): the 22 P5 files add 1056 s of summed case time on lavapipe (the property suites alone 104 s, section 2 item 15), spread over vitest's workers on the 32-core box. The leg was rerun on the final tree (`node-lavapipe-cov-2.log`): 242 s, exit 1, the same error at the same place, so it is reproducible. Two discriminating runs found the cause: the same command WITHOUT `--coverage` runs in 59 s, exit 0, no error (`node-lavapipe-nocov.log`: 515 s of summed case time against 1048-1056 s with coverage -- v8's precise coverage roughly doubles the CPU-heavy f64 oracle and fast-check work of the P5 suites), and WITH `--coverage --maxWorkers=3` (the CI runner has 4 cores) it runs in 298 s, exit 0, no error, the same 95.38 / 94.7 / 98.82 / 95.38 coverage (`node-lavapipe-cov-mw3.log`). The RPC timeout is therefore a saturation effect of the dev box's default fork count (one fork per core on 32 cores, each fork running lavapipe's own thread pool, the workers' JS twice as slow under coverage): the main process missed one worker's 60 s `onTaskUpdate` deadline. Nothing in a test is involved and T-12's 900 s budget holds at every worker count measured. | It appeared on the lane: the T4 run 35547623119 of PR 16 (a 4-core machine.dev runner, `--project=node --project=node-limits`, no coverage) exited 1 with 95 files / 1840 tests green and THREE `onTaskUpdate` timeouts, 442 s wall for 1240 s of summed case time. `vitest.config.ts` now sets `maxWorkers: availableParallelism() - 1` on both node projects (one core for the main process; `--maxWorkers` on the command line still overrides it), the cap this row recommended, taken as the owner decision of 2026-09-21 rather than touching a test. The cap did NOT clear it: the next run (35548431052, three forks) exited 1 with two timeouts, and the log's silent gaps place both inside tests that run the f64 oracles synchronously for 60-77 s on the runner's CPU (the FR distributional admission rule, the FA2 free-running traces on random1k). The diagnosis above was wrong in one respect: the deadline is missed by the WORKER, whose event loop is blocked by its own synchronous oracle work, not by a starved main process, so the fork count is beside the point. The tests are the fix after all: the trajectory-sensitivity ensembles, the distributional admission layouts and the FA2 trace helper await a macrotask between oracle iterations (`yieldToEventLoop` in `test/helpers/fa2-parity.ts`), so no synchronous stretch outlasts one iteration; the fork cap stays as a harmless default |
| G5-F3 | Root `pnpm exec knip` exits 1 (pre-existing, G7-F4: 17 unused files across the monorepo, one under this package, `benchmarks/run.ts`, plus its two `harness.ts` imports). Before this task it also listed twelve P5 names under this package: three unused exports (`readStateScalar` in `test/helpers/fr-parity.ts`, `createSeSim` in `test/helpers/se-parity.ts`, both used only inside their file, and `fruchtermanReingoldOracle` in `test/oracle/fruchterman-reingold.ts`, a scene-unit wrapper no suite calls) and nine unused exported types (`FrSim`, `TrajectorySensitivity`, `FrStageResult`, `FrStageCapture`, `SeSim`, `SeStageResult`, `SeStageCapture`, `FrOracleStages`), all used only inside their own file. This task dropped the `export` keyword on the eleven local names and deleted the dead wrapper with its `F32` import (34 lines); `pnpm run lint` is green and knip reports nothing else under the package. Those three files belong to Tasks P5-T3 / P5-T4 in the plan's file list; the edits change no behaviour. | none needed; G7-F4's `benchmarks/run.ts` entry stays open |
| G5-F4 | The package is 0.5.0, not the 0.4.1 the plan's section 0.1 recorded: `5ba15bb7 chore(release): publish [skip ci]` landed on the branch base after the plan was written. Nothing in the phase reads the version. | none needed |
| G5-F5 | Eleven of the 46 tolerances are clipped at the design's cap with a factor under 10 over their noise row: `fr-displacement` (1e-4, 5.02x), `fr-force-parity` (1e-4, 4.19x), `fr-force-sum` (1e-4, 4.19x), `fr-trajectory` (1e-3, 5.50x), `fr-distributional` (0.1, 4.55x), `fr-distributional.cross` (0.2, 8.70x), `se-inspect.positions` and `se-inspect.scene` (1e-4, 3.80x), their `.cross` twins (1e-4, 8.86x) and `se-twins.positions` (1e-5, 1.75x). None is ABOVE its cap, so spec 10.4's re-fix clause is not triggered; the record notes them because the tightest, `se-twins.positions`, leaves only 1.75x between the measured no-subgroups fold error (5.703e-6 on NVIDIA) and the 1e-5 cap the FA2 twins share. | none needed |
| G5-F6 | The P5 noise rows have node classes only (`nvidia-lovelace-node`, `mesa-software-node`, `oracle-f64`, the twins): no browser test writes an FR / spring noise fixture (the browser smoke asserts behaviour, not a floor), so the cross-adapter rows cannot include SwiftShader or NVIDIA-Chromium, as G7-F3 already recorded for `spmv-pull`. | `{{OPEN: accept node-only, or add a browser writer for the two models}}` |
| G5-F7 | `LC_ALL=C grep -rnP '[^\x00-\x7F]' src test benchmarks docs | wc -l` prints 0 but grep also reports `test/fixtures/rich-v1.gsnp: binary file matches`: the graph-format wire fixture is binary and pre-dates P5. No text file under the four directories carries a non-ASCII byte. | none needed |
| G5-F8 | Counts that differ from the plan's template: `se-properties.test.ts` has 8 rows (the plan's "7 + 7"; Task P5-T5 pinned the run-level `maxIter` cap as its own row), the `fr-layout-oracle` noise member is karate WITH the fixed mask at horizon 10 (Task P5-T4: free karate is not admitted at 10), and the story-graph settle count differs per adapter (496 on NVIDIA-Dawn and NVIDIA-Chromium, 480 on lavapipe and SwiftShader: the f32 arithmetic of the two adapters differs and the shared rule fires on a threshold), every count under the 1,000 of the checklist. | none needed |
| G5-F9 | The plan sums P5 to 15.5 ed against the design's 4-5 (plan section 0.6, the reasons stated there). Every task landed; whether the design cell stands as the estimate it was, or the phase is split for the PR (plan 0.6's proposal), is the owner's call. | `{{OPEN: owner}}` |
| G5-F10 | Dawn on Metal (hosts.yml run 35548431083 of PR 16, macos-latest, the plain and the no-subgroups pass alike) failed two P5 assertions that NVIDIA, lavapipe, SwiftShader and WARP pass: `fr-layout-oracle` grid10/k=auto at horizon 10, 3.743e-4 against the tolerance 1.813e-4 (10x the NVIDIA floor), on a horizon the admission rule had let through with the f32 oracle already 1.817e-4 from the f64 (a third of the cap is 3.333e-4); and `se-distributional` karate/2d, a metric difference of 1.902e-2 against the tolerance 1.267e-2, on the case whose own eight-perturbation oracle spread is 2.941e-2 -- the admission rule bounds the spread by a third of the CAP (3.333e-2), which is above the tolerance the case's floor derives, so a differently rounding adapter can land inside the spread and outside the tolerance. Both are the admission rules leaving less margin than another adapter's rounding takes, not a kernel fault: every other horizon and case of both suites passed on Metal with ratios under 0.6. | grid10/k=auto is asserted at 1 and 5 and printed at 10; karate/2d stays the se-distributional noise member (its floor row is measured on it) and its difference is printed, not asserted, while karate/3d and random1k/3d (Metal 1.968e-6 and 2.902e-4) carry the assertion. The admission rules themselves are unchanged; a rule that bounds the spread by the derived tolerance rather than the cap is the P4 gate's to consider, where the exact-vs-grid caps meet the same question (P4 plan PD-20) |
| G5-F11 | The CI lane's `webgpu-graph-algorithms-node` shard (ci.yml run 35548431040 of PR 16: lavapipe on a 4-core runner, `--coverage`) failed ONE test: the `se-distributional` admission ensemble timed out at its 600 s case timeout (654 s), every other test green. The five P5 admission ensembles (fr-trace, se-trace, fr-distributional, se-distributional, fr-layout-oracle) ran the f64 oracles at scale 1 on every adapter -- nine 100-iteration random1k layouts per candidate under v8 coverage on that CPU -- where the P3 suite (`fa2-distributional.test.ts`) runs its admission rule at `gpuScale()` like every other fixture (spec 11.2). | The five ensembles run at `gpuScale()`; on a software adapter (1/50) their numbers are printed and not asserted, since the recorded admissions are full-scale facts of the pure-JS oracles that the hardware lanes assert; the record-consistency checks (the noise member is admitted, the fixture names) stay unconditional |

Signed off: `{{OPEN: owner, 2026-09-DD}}`.
