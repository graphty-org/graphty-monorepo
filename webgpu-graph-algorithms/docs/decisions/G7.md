# G7 -- the SpMV family and WCC gate (spec 13 row P7)

Recorded by: Task M8b-T12 of `design/webgpu/plans/2026-09-19-webgpu-m8b-gpu-spmv.md`, 2026-09-19 (every
measurement below ran on the dev box on 2026-09-19 local time; the benchmark session date is UTC). The owner signs
section 7. Commits: the fifteen of the M8b PR -- `{{OPEN: none committed at the time of writing; the owner lands them
through tools/commit-changes.sh and pastes the short hashes here}}`.
Environment: Node v22.22.1, pnpm 10.0.0, vitest 3.2.7, webgpu 0.4.0 (Dawn), Ubuntu 22.04.5 LTS, NVIDIA driver
580.173.02, Mesa lavapipe 23.2.1 (LLVM 15.0.7), Playwright 1.57.0 with Chromium 143.0.7499.4 (headless shell 1200).
Every command ran from `webgpu-graph-algorithms/` in the worktree `.worktrees/webgpu-spmv-wcc` (branch
`feat/webgpu-spmv-wcc`); the logs are under `tmp/m8b/t12/` (gitignored).

The rule of this record (spec 10.4): every number is measured and names the command or the file it came from; a
missed target is never relaxed here -- it is re-fixed by an owner decision in section 7, or the phase stays open.

GATE STATUS: GREEN on the dev box (every item of section 2 passes on NVIDIA-Dawn, on lavapipe and in Chromium), with
TWO items open: the GPU lane (`gpu-linux-t4`) has not run this branch (rule (a) of spec 13 wants both lanes; the rows
of sections 1, 3 and 6 that need it carry an `{{OPEN: ...}}` marker with the command that fills them, exactly as G0
and G2 did before the lane existed), and T-9 misses its target on the dev box (section 3), which spec 10.4 leaves to
the owner in section 7.

## 0. What the phase built (the deliverable of spec 13 row P7)

Grid-stride dispatch (`planGridStride`), `packViews` and the `reverse()` / `edgeList` residency views, the `spmv-pull`
kernel over pre-scaled `xNorm` (thread-per-row tier only, `design/decisions/2026-09-19-spmv-tier-zero-only.md`), the
device out-weight normaliser, PageRank and personalized PageRank with `pr-scale` / `pr-finalize` and
`firstConvergedIteration`, HITS, eigenvector and Katz centrality over one power-iteration driver, Afforest WCC over
`wcc-link-sample` / `wcc-link-edges` / `wcc-compress` / `wcc-sample` with `renumberPartition` on readback, the seven
`GpuAccelerator` algorithm members, four f64 oracles, the `pagerank` / `wcc` benchmark groups, eighteen sabotage
mutations, the first `node-limits` test and the browser smoke. The five departures from the design are recorded in
`design/decisions/2026-09-19-{spmv-pull-is-its-own-kernel,spmv-tier-zero-only,afforest-needs-no-dedupe,outweightsum-is-call-scratch,pagerank-ping-pong-is-two-buffers}.md`.

## 1. Adapters exercised

| Adapter | Runtime | adapter class | runner class | subgroups | how it was run |
| --- | --- | --- | --- | --- | --- |
| NVIDIA GeForce RTX 4070 SUPER, Dawn (driver 580.173.02) | node | nvidia-lovelace-node | nvidia-lovelace-driver580 | 32 / 32 | `LD_LIBRARY_PATH=<the libEGL tree> GRAPHTY_GPU_REQUIRE=hardware XDG_RUNTIME_DIR=/tmp pnpm exec vitest run --project=node` (also with `GRAPHTY_GPU_NO_SUBGROUPS=1` over `test/primitives test/layouts test/algorithms`, and `--project=node-limits`) |
| Mesa lavapipe (Mesa 23.2.1, LLVM 15.0.7), Dawn | node | mesa-software-node | -- (a software adapter is never timed) | 8 / 8 | the default-lane env: `GRAPHTY_GPU_ADAPTER=llvmpipe GRAPHTY_GPU_REQUIRE=any VK_DRIVER_FILES=/usr/share/vulkan/icd.d/lvp_icd.x86_64.json XDG_RUNTIME_DIR=/tmp pnpm exec vitest run --project=node --coverage` |
| Chromium 143 on SwiftShader | browser | google-swiftshader-browser | -- | 4 / 4 | `GRAPHTY_BROWSER_GPU=swiftshader GRAPHTY_GPU_REQUIRE=any node scripts/run-browser-project.js` |
| Chromium 143 on the RTX 4070 SUPER | browser | nvidia-lovelace-browser | -- | 32 / 32 | `LD_LIBRARY_PATH=<the libEGL tree> GRAPHTY_BROWSER_GPU=nvidia GRAPHTY_GPU_REQUIRE=nvidia node scripts/run-browser-project.js` (11 files, 48 passed of 48, 15.78 s, `bench.test.ts` included) |
| GitHub gpu-linux-t4 (NVIDIA Tesla T4, a machine.dev runner) | node + browser | pending | gpu-linux-t4 | pending | `{{OPEN: push feat/webgpu-spmv-wcc, open the PR with the gpu label so gpu.yml runs; paste the run id here}}` |

The libEGL tree is `/home/apowers/Projects/graphty-monorepo/tmp/egl/root/usr/lib/x86_64-linux-gnu` (`CLAUDE.md`,
"Running the suites locally"); without it Dawn lists no hardware adapter and `GRAPHTY_GPU_REQUIRE=hardware` fails up
front in the global setup with `E_SOFTWARE_ONLY`.

## 2. The G7 checklist (spec 13 row P7), each item mapped to its evidence

The whole `node` project: NVIDIA-Dawn 69 files, 1569 passed, 1 skipped of 1570, 97.78 s (`tmp/m8b/t12/node-nvidia.log`);
lavapipe with `--coverage` 69 files, 1569 passed, 1 skipped of 1570, 66.65 s (`tmp/m8b/t12/node-lavapipe.log`);
the `GRAPHTY_GPU_NO_SUBGROUPS=1` pass on NVIDIA over `test/primitives test/layouts test/algorithms` 22 files, 558
passed, 12 skipped of 570, 94.51 s (`tmp/m8b/t12/node-nvidia-nosub.log`); `node-limits` on NVIDIA 1 passed
(`test/limits/pagerank-1m.test.ts`, 3.02 s for the 1M / 10M PageRank, `tmp/m8b/t12/node-limits-nvidia.log`);
`pnpm run build:all && pnpm run lint` green (eslint plus the two tsc runs); `LC_ALL=C grep -rnP '[^\x00-\x7F]' src test
benchmarks docs | wc -l` = 0. The per-item maxima below are from a one-off report test that ran the suites' own
comparisons over every fixture on both adapters (`tmp/m8b/t12/report-nvidia.log`, `tmp/m8b/t12/noise-lavapipe.log`)
and was deleted afterwards; the suites assert the bounds, the report only prints the maxima.

| # | Item | Evidence | Result | Status |
| --- | --- | --- | --- | --- |
| 1 | 9.7 parity on all fixtures: <= 1e-5 relative, top-k, `iterations` +-1, `converged` identical | `test/algorithms/pagerank.test.ts` (22 GPU cases + 3 pure oracle pins), `test/algorithms/spectral.test.ts` (49 GPU cases + 3 pure) over the 13 named fixtures, each algorithm after exactly 8 iterations against its f64 oracle, then to convergence | max relative error after 8 iterations, NVIDIA / lavapipe: pageRank 4.596e-7 / 2.891e-7, HITS hubs 1.245e-7 / 1.193e-7, HITS authorities 1.245e-7 / 1.193e-7, eigenvector 2.295e-7 / 1.392e-7, Katz 1.718e-7 / 1.573e-7 (all under 1e-5); `converged` identical on every fixture for all four; `iterations` differ by at most 1 (pageRank on `one`, Katz on `karate`; 0 for HITS and eigenvector); top-10 identical on karate, random1k and hub10k (pageRank) and karate, random1k (the three spectral legs) | pass |
| 2 | weighted with zero-weight arcs and dangling nodes; directed and undirected | `pagerank.test.ts` "weighted with zero-weight arcs (parallel) and a directed sink; weighted: false folds 1 and differs from the weighted run" and "directed and undirected forms of the same edge set each match their oracle and differ from each other"; `spectral.test.ts` "weighted: false on parallel equals the oracle with all weights 1" and "directed and undirected forms ... directed HITS separates hubs from authorities"; `test/primitives/spmv.test.ts` "zero-weight arcs (parallel) contribute exactly 0" and "the dangling leg" | every leg within 1e-5 of its oracle on both adapters; `danglingMass` within 1e-5 of the oracle's on the directed sink; the unweighted run differs from the weighted by > 1e-5 and the directed from the undirected by > 1e-2 (so the legs are not vacuous) | pass |
| 3 | personalization one-hot and uniform | `pagerank.test.ts` "personalization: one-hot e_0 matches the oracle; uniform 1/n equals plain pageRank within 1e-6" (karate) | one-hot max relative error after 8 iterations 3.688e-7 (NVIDIA) / 2.969e-7 (lavapipe); the uniform vector's scores are bitwise the plain run's (0.000e+0) on both; a converged one-hot run keeps node 0 on top and matches the oracle's `converged` and `iterations` +-1 | pass |
| 4 | the pull kernel binds exactly the 8 of 8.2; every 8.10 kernel matches its count | `test/kernel/bind-group-budget.test.ts` (the storage-binding counts per kernel, read-only and read-write together) | spmv-pull 8, pr-scale 5, pr-finalize 1, wcc-link-edges 3, wcc-compress 1, wcc-sample 2; wcc-link-sample (absent from the 8.10 table) 5 = the four graph slots plus the atomic `comp` | pass |
| 5 | no host readback inside a batch of 8 iterations (`mapAsync` count) | `pagerank.test.ts` "G7 leak clause: a 100-node run pinned to 8 iterations maps exactly ONE staging buffer, and leaves no buffer alive"; `spectral.test.ts` "PD-10 leak clause: 8 iterations on 100 nodes map exactly ONE staging buffer per run"; `test/helpers/leak-counter.ts` | pageRank 1, eigenvector 1, Katz 1, HITS 2 (its two interleaved chains, one readback each); no buffer alive after `release()` | pass |
| 6 | SpMV twin identical in-process | `test/primitives/spmv.test.ts` "twins in-process: the no-subgroup context gives bitwise the same result (spmv-pull calls no reduction helper)"; the `GRAPHTY_GPU_NO_SUBGROUPS=1` pass over `test/algorithms` (which is what exercises `pr-scale` / `pr-finalize`, the two P7 kernels that declare `needs: ["subgroups"]`) | bitwise on both adapters; the twin pass 558 passed, 12 skipped (the skips are the twin cases that need the feature the pass disables) | pass |
| 7 | WCC partition equality after renumbering, incl. directed treated weakly, singletons, giant + dust, `arcCount === 0` | `test/algorithms/components.test.ts` (23 GPU cases + 1 pure oracle pin): labels identical to the union-find oracle on all 13 fixtures; "directed input is treated weakly"; "singletons: every isolated node of the isolated fixture is its own block"; "a giant component plus dust: a 10k random graph unioned with 500 two-node components"; "arcCount 0: every node its own block and count n, with only rowPtr resident"; "n = 0"; "renumber: false returns the raw roots, partition-equal to the oracle"; "groups() ... a permutation of [0, n)"; "self-loop and parallel fixtures ... hand-computed labels"; "two runs give identical labels" | every label array identical to the oracle's (not merely partition-equal) after renumbering, on both adapters | pass |
| 8 | browser smoke (4) green | `test/browser/algorithms.test.ts` on SwiftShader and NVIDIA: `pageRank` on karate within 1e-5 of the oracle after 8 iterations, `connectedComponents` on karate identical to the union-find oracle -- two `it`s; the plan (Task M8b-T10 Step 3) leaves design 11.6 item 4's BFS case to P8, which is why the smoke is two cases and not four | SwiftShader: the browser project 10 files passed, 1 skipped (`bench.test.ts`, GPU lane only), 47 passed, 1 skipped of 48, 31.94 s (`tmp/m8b/t12/browser-swiftshader.log`); the P7 compile-matrix cases (`pr-scale`, `pr-finalize` twins on both feature settings) compile; NVIDIA-Chromium (`isFallbackAdapter=false`, subgroups 32 / 32): 11 files passed, 48 passed of 48 including `bench.test.ts`, 15.78 s (`tmp/m8b/t12/browser-nvidia.log`) | pass |
| 9 | sabotage: every P7 kernel's mutations break their check by >= 10x | `test/sabotage/spmv.test.ts`, `test/sabotage/wcc.test.ts` (eighteen mutations, three per kernel; `wcc-sample` is `SABOTAGE_EXEMPT`, PD-6) | worst (smallest) factor per kernel, identical on NVIDIA and lavapipe: spmv-pull 5.98e+4 (`dangling-dropped`; the other two 4.42e+5), pr-scale 6.18e+4 (`dangling-not-accumulated`; the other two Infinity), pr-finalize Infinity (all three), wcc-link-sample 5.21e+2 (`degree-guard-inclusive`; the other two 9.60e+2), wcc-link-edges 9.00e+2 (all three), wcc-compress 9.98e+2 (two rows; `root-not-stored` Infinity, an `E_VALIDATION` after the 64-round cap); the unmutated controls measure 4.30e-2 (spmv) and 9.81e-2 / 1.01e-1 (pr-scale / pr-finalize, NVIDIA / lavapipe), i.e. under 1 | pass |

## 3. T-8 / T-9 (spec 10.4; `benchmarks/results/nvidia-lovelace-driver580.json`, session 2026-09-20T01:06:48.656Z)

The dev box (RTX 4070 SUPER, the card verified idle before the capture), medians of 5 runs, wall end to end from a
released core (the upload is inside the row). The `pagerank` rows run all 100 iterations (`tolerance: 0`): at the
NetworkX tolerance of 1e-6 the seeded G(n, m) input converges from the uniform start in one to four iterations, which
would time one pull and call it a hundred (`benchmarks/pagerank.bench.ts`).

| Id | Benchmark (group / name) | Target | Measured median of 5 (ms) | min / max (ms) | Rate | Pass |
| --- | --- | --- | --- | --- | --- | --- |
| T-8 | pagerank / pagerank 100 iterations at 100k/1M | <= 150 ms | 17.204 | 16.797 / 17.571 | 5.81e+6 nodes/s | yes |
| T-8 | pagerank / pagerank 100 iterations at 1M/10M | <= 1.5 s | 198.645 | 192.311 / 201.516 | 5.03e+6 nodes/s | yes |
| T-9 | wcc / wcc at 1M/10M | <= 100 ms | 145.970 | 143.464 / 148.130 | 6.85e+6 nodes/s | no |
| -- | pagerank / pagerank 100 iterations at 10k/100k (no target) | -- | 3.847 | 3.369 / 4.144 | 2.60e+6 nodes/s | -- |
| -- | wcc / wcc at 100k/1M (no target) | -- | 12.613 | 12.422 / 14.381 | 7.93e+6 nodes/s | -- |

The T-9 miss is the upload: the row is wall end to end from a released core, so it carries the 164 MB hot-prefix
upload that the `upload` group's T-1 row times at 125.738 ms on its own (a target T-1 already misses, the open owner
decision of `G1.md` section 7); with the core resident the same `connectedComponents` call takes 11-16 ms
(`README.md`, "Benchmarks"). The target is therefore below the upload it includes; the owner decision is in section 7.

The GPU lane (`benchmarks/results/gpu-linux-t4.json`) has no `pagerank` or `wcc` session yet:

| Id | Benchmark (group / name) | Target | Measured median of 5 (ms) | min / max (ms) | Rate | Pass |
| --- | --- | --- | --- | --- | --- | --- |
| T-8 | pagerank / pagerank 100 iterations at 100k/1M | <= 150 ms | `{{OPEN: gpu-linux-t4}}` | | | |
| T-8 | pagerank / pagerank 100 iterations at 1M/10M | <= 1.5 s | `{{OPEN: gpu-linux-t4}}` | | | |
| T-9 | wcc / wcc at 1M/10M | <= 100 ms | `{{OPEN: gpu-linux-t4}}` | | | |

Owner procedure that fills them (the plan's Task M8b-T9 Step 3): (1) push `feat/webgpu-spmv-wcc` and open the PR
with the `gpu` label so `gpu.yml` runs; its `pnpm run bench` step reports every `pagerank` / `wcc` row as `new (no
baseline)`, which `scripts/bench-compare.js` rule 3 passes -- a FAILED compare is a finding about `upload`,
`roundtrip` or `layout-exact` on this branch, read first; (2) `gh run download <run id> -n gpu-results-node -D
tmp/m8b/t4` (from the main checkout; expect `benchmarks/out/gpu-linux-t4.json` among the files); (3) from
`webgpu-graph-algorithms/`, `node tmp/m8b/append-session.mjs <that file> benchmarks/results/gpu-linux-t4.json` (the
script refuses a session missing any of the five groups); (4) `node tmp/m8b/readme-table.mjs
benchmarks/results/gpu-linux-t4.json` and paste the rows into `README.md`'s "The CI lane (gpu-linux-t4)" section and
into the table above, keeping that section's Chromium T-5 figure.

## 4. Cross-adapter results (spec 11.5, 11.9; `benchmarks/results/noise-floor.json`)

The phase's noise-floor row (spec 13 rule (f)) is the `spmv-pull` pull over the weighted random1k snapshot
(1000 nodes, 5000 edges, seed 7; `x` seeded 42; the PageRank coefficients without the dangling term), recorded by
`test/primitives/spmv.test.ts` under `GRAPHTY_NOISE_FLOOR_WRITE=1` on each adapter in turn (NVIDIA, then lavapipe,
then NVIDIA again for the reverse cross row); the fixtures are
`test/fixtures/noise/spmv-pull-random1k-{nvidia-lovelace-node,mesa-software-node}.json`. The tolerance
`spmv-pull.cross` = 1.699e-6 is 10x its basis row `spmv-pull.oracle-f64.nvidia-lovelace-node`, the same construction
as `segmented-reduce.cross`. The measured cross-adapter error (1.397e-7) is 12x under it. Both adapters' pulls are at
the Kahan floor of the f32 fold (the T6 fix of `spmv-pull.wgsl.ts`: the compensation is kept opaque to the compiler
through `select`, so the sum is no longer the naive f32 sum on either compiler).

| Row id | kernel / fixture | comparison | a | b | maxRelError | maxAbsError | samples |
| --- | --- | --- | --- | --- | --- | --- | --- |
| spmv-pull.oracle-f64.nvidia-lovelace-node | spmv-pull / random1k | oracle-f64 | nvidia-lovelace-node | oracle-f64 | 1.699e-7 | 2.555e-6 | 1000 |
| spmv-pull.oracle-f64.mesa-software-node | spmv-pull / random1k | oracle-f64 | mesa-software-node | oracle-f64 | 1.874e-7 | 3.271e-6 | 1000 |
| spmv-pull.cross.nvidia-lovelace-node--mesa-software-node | spmv-pull / random1k | cross-adapter | nvidia-lovelace-node | mesa-software-node | 1.397e-7 | 1.907e-6 | 1000 |
| spmv-pull.cross.mesa-software-node--nvidia-lovelace-node | spmv-pull / random1k | cross-adapter | mesa-software-node | nvidia-lovelace-node | 1.397e-7 | 1.907e-6 | 1000 |

No browser row: the browser project has no `spmv-pull` writer (the compile matrix writes `segmented-reduce` only), so
the `google-swiftshader-browser` and `nvidia-lovelace-browser` fixtures the other kernels carry do not exist for this
one (finding G7-F3). The committed state was re-verified without the write flag on both adapters afterwards
(`test/noise-floor.test.ts` + `test/primitives/spmv.test.ts`: 53 passed on each; `tmp/m8b/t12/noise-verify-*.log`).

## 5. Coverage (spec 11.8; the default-lane run of Step 1)

| lines | functions | branches | statements | threshold | wall time |
| --- | --- | --- | --- | --- | --- |
| 95.24 % | 94.55 % | 98.72 % | 95.24 % | 80 / 80 / 75 / 80 (`vitest.config.ts`, the `node` project) | 66.65 s (lavapipe, `--coverage`); 97.78 s on NVIDIA without coverage |

## 6. Baselines committed

- `benchmarks/results/nvidia-lovelace-driver580.json`: a third session (2026-09-20T01:06:48.656Z, driver
  580.173.02) carrying every group -- `upload`, `roundtrip`, `layout-exact` and the new `pagerank` (three rows) and
  `wcc` (two rows); it is the file's last session and therefore the baseline `bench:compare` reads.
- `benchmarks/results/gpu-linux-t4.json`: UNCHANGED -- its one session (2026-09-18T07:03:17.146Z) predates the P7
  groups. `{{OPEN: append the gpu-linux-t4 session by the procedure of section 3}}`. Until then `bench:compare` on the
  lane reports the five new rows as `new (no baseline)` and passes them.
- `benchmarks/results/noise-floor.json`: four `spmv-pull` rows and the `spmv-pull.cross` tolerance (section 4); no
  other row or tolerance changed.
- `test/fixtures/noise/spmv-pull-random1k-nvidia-lovelace-node.json`,
  `test/fixtures/noise/spmv-pull-random1k-mesa-software-node.json`: new.
- `README.md`: the T-table rows T-8 / T-9 for the dev box (Task M8b-T9); the "CI lane (gpu-linux-t4)" section still
  lacks them (the same OPEN item).

## 7. Findings, owner decisions, re-fixed targets

| Id | Finding | Owner decision |
| --- | --- | --- |
| G7-F1 | T-9 misses: 145.970 ms against 100 ms at 1M / 10M. The row includes the 164 MB upload T-1 times at 125.738 ms alone; the resident-core call is 11-16 ms, so the algorithm is inside the target by a factor of six and the target is below the upload it contains. Spec 10.4 leaves two honest exits: re-fix the target (either as "from a resident core" -- the number the algorithm controls -- or as T-1 + the resident cost), or keep the phase open until T-1 itself is fixed (G1.md section 7). The "Pass" column says `no` either way. | `{{OPEN: owner}}` |
| G7-F2 | The GPU lane has not run this branch: section 1's last row, section 3's second table and the `gpu-linux-t4.json` bullet of section 6 are `{{OPEN}}`. Rule (a) of spec 13 (green on both lanes) is not met until the labelled PR's `gpu.yml` run is green and the owner fills them. | `{{OPEN: owner}}` |
| G7-F3 | `spmv-pull` has node fixtures only: no browser test writes a `spmv-pull` noise fixture, so the cross-adapter row cannot include SwiftShader or NVIDIA-Chromium (every earlier kernel row has both). A `spmv-pull` case in `test/browser/compile-matrix.test.ts` that pulls the same random1k and calls `writeNoiseFixture` would close it; deferred to the owner because the plan's browser deliverable (Task M8b-T10 Step 3) is the two-case smoke. | `{{OPEN: accept node-only, or add the browser writer}}` |
| G7-F4 | Root `pnpm exec knip` exits 1: 17 unused files across the monorepo, ONE under this package, `benchmarks/run.ts`, plus its two `harness.ts` imports `printTable` / `gpuSessionInfo` reported unused as a consequence. Pre-existing: HEAD's `run.ts` imports the same two names, `knip.config.ts` lists `benchmarks/layout-run.ts` as the package's only benchmark entry and does not read the `bench` script, and `graph-format/benchmarks/run.ts` and `graph-io/benchmarks/run.ts` are reported the same way. One-line fix: add `benchmarks/run.ts` to that entry list (outside this phase's files). | `{{OPEN: owner}}` |
| G7-F5 | The "browser smoke (4)" of the checklist is two `it`s: design 11.6 item 4's third case (BFS) is P8's, and the plan says so (Task M8b-T10 Step 3). No fourth case is named anywhere. Recorded so the count is not read as a gap. | none needed |
| G7-F6 | A converged-run sweep of top-10 order over ALL 13 fixtures (the report test behind section 2, not a suite case) shows index-order differences among TIED scores on grid10 (both adapters) and path1k, hub10k, isolated (NVIDIA): symmetric fixtures whose tied f64 scores round to different f32 neighbours. The checklist's top-k item is on the fixtures whose scores are distinct (karate, random1k, hub10k at 8 iterations), where the order is identical; a tie-aware comparison would be needed to extend the item to the symmetric fixtures. | none needed |
| G7-F7 | The hardware prefix `GRAPHTY_GPU_REQUIRE=hardware XDG_RUNTIME_DIR=/tmp` alone fails up front on this box (`E_SOFTWARE_ONLY`: Dawn lists only llvmpipe); every hardware run above adds `LD_LIBRARY_PATH` to the libEGL tree, as `CLAUDE.md` documents. | none needed |
| G7-F8 | The plan sums M8b to 15.5 ed against the design's 8-10 (plan section 0.6). Every task landed; whether the design cell stands as the estimate it was, or the phase is split for the PR, is the owner's call named there. | `{{OPEN: owner}}` |

Signed off: `{{OPEN: owner, 2026-09-DD}}`.
